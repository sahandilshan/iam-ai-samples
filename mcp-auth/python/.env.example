# ==============================================
# MCP Server - OAuth Configuration
# ==============================================
# Only 3 environment variables required!

# 1. OAuth Issuer URL (e.g., https://api.asgardeo.io/t/<tenant>/oauth2/token)
AUTH_ISSUER=https://api.asgardeo.io/t/<tenant>/oauth2/token

# 2. OAuth Client ID
CLIENT_ID=<CLIENT_ID>

# 3. JWKS URL for token verification (e.g., https://api.asgardeo.io/t/<tenant>/oauth2/jwks)
JWKS_URL=https://api.asgardeo.io/t/<tenant>/oauth2/jwks

# Optional: MCP Server URL (defaults to http://localhost:8000 if not set)
# MCP_SERVER_URL=http://localhost:8000
